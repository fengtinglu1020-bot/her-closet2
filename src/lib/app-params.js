// Base44 app params are no longer used (Base44 SDK disabled).
// Kept as an empty stub so any stray import does not crash.
export const appParams = {
  appId: null,
  token: null,
  fromUrl: null,
  functionsVersion: null,
  appBaseUrl: null,
};
